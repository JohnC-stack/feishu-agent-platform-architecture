# 飞书 Agent 平台实施计划

## 1. 计划基线

- 基线日期：2026-08-20
- 目标周期：20 周
- 交付方式：先完成只读试点，再逐步开放受审批保护的写操作
- 生产形态：Windows 原生执行面 + Hyper-V Linux VM / Docker 控制面
- 计划状态：可执行基线；后续开发、测试和验收均按本计划推进
- 执行状态：见 [IMPLEMENTATION_STATUS.md](IMPLEMENTATION_STATUS.md)

若飞书应用、服务账号、内网白名单、Windows 管理员权限或安全审批不能按期提供，日历时间可能额外延长 2–6 周。等待外部权限不改变开发顺序，但会影响联调和生产验收日期。

### 1.1 环境前置与阶段门禁

所有阶段严格使用以下执行规则：

1. 在执行阶段任务前先核对操作系统、运行时、包管理器、容器、网络、端口、权限和外部依赖；缺失项先安装或修复并完成版本与可用性验证。
2. 阶段内任一安装、构建、测试、扫描、健康检查或验收命令失败时，立即停留在当前阶段；定位并修复后，从受影响环节开始重新验收。
3. 只有阶段内全部任务、自动化测试、运行态检查、安全检查和文档证据均通过，才允许更新阶段状态并进入下一阶段。
4. 需要远端系统、账号、审批或硬件的项目保持“待外部验收”，不得用本地模拟结果替代，也不得提前开始依赖该结果的下一阶段工作。
5. 每次阶段出口至少记录环境版本、执行命令、退出状态、关键健康结果和未关闭项；“已安装”“已启动”不能替代实际可用性验证。

## 2. 项目目标

平台需要达到以下结果：

1. 飞书通过 WSS 443 长连接接收事件，不开放公网回调端口。
2. 明确命令和固定查询由普通程序直接执行，不调用模型。
3. 自然语言跨系统只读任务可通过 API/ReAct 执行器完成；该通道受独立功能开关控制，当前关闭。
4. 代码、本地文件、CLI 和重型复杂任务通过 Agent CLI 执行器完成。
5. GitLab、Confluence、飞书和公司业务系统统一经过工具网关访问。
6. 身份、权限、审批、幂等、Token 配额、敏感信息和审计由平台统一治理。
7. 提供完整 Web 管理台、任务可视化、运维监控、告警、备份、恢复和版本回滚能力。
8. Windows Agent 工作区按任务隔离，高风险任务使用 Hyper-V 强隔离环境。

## 3. 交付范围

### 3.1 核心平台

- 飞书 WSS Gateway、事件解析、去重、限流和幂等。
- Redis + BullMQ 异步任务、并发控制、超时、重试和死信队列。
- 规则任务路由器、会话上下文和统一回复调度器。
- Executor Adapter 及统一事件、取消、超时和结果协议。
- PostgreSQL 配置、会话、任务、审批、审计和成本数据模型。

### 3.2 三类执行器

| 执行器               | 处理范围                                     | 模型使用     |
| -------------------- | -------------------------------------------- | ------------ |
| `DirectToolExecutor` | 明确命令、固定查询、REST、`glab`、专用 CLI   | 0 模型 Token |
| `ApiAgentExecutor`   | 自然语言跨系统组合、摘要、只读分析；当前关闭 | 按任务产生   |
| `AgentCliExecutor`   | 代码、本地文件、测试、CLI 和重型复杂任务     | 按任务产生   |

### 3.3 企业系统接入

- GitLab：项目、仓库、MR、评论、流水线和作业日志。
- Confluence：CQL 搜索、页面、附件、评论和受审批保护的写入。
- 飞书 OpenAPI：消息、卡片、文档、多维表格和审批交互。
- 公司业务系统：通过白名单 REST API 或专用 CLI 接入。
- MCP：仅在当前任务确实需要时加载，不作为所有任务的默认路径。

### 3.4 管理和运维页面

1. 平台总览：在线状态、任务量、成功率、P95 耗时、Token 和成本。
2. 任务中心：任务时间线、输入、路由、工具调用、结果、错误和重试。
3. 会话中心：群、用户、上下文摘要和会话隔离状态。
4. 队列与 Worker：等待、运行、失败、重试、取消和死信任务。
5. 执行器与沙箱：实例状态、并发、工作区、超时和资源使用。
6. 系统集成：飞书、GitLab、Confluence、模型及业务系统健康状态。
7. 审批中心：写操作申请、风险信息、批准、拒绝和过期处理。
8. 用户与权限：用户、群组、角色、工具授权和配额。
9. Token 与成本：按用户、群、任务、模型和时间维度统计。
10. 日志与 Trace：检索、关联 ID、调用链和错误详情。
11. 告警中心：规则、通知目标、静默、恢复和处理记录。
12. 配置中心：路由规则、模型策略、功能开关和脱敏后的凭据状态。
13. 发布与备份：版本、健康检查、备份、恢复演练和回滚。

管理台不得显示明文密钥、Cookie、访问令牌或用户隐私数据。凭据页面只显示引用、状态、更新时间和权限范围。

## 4. 目标工程结构

第一阶段按以下单体仓库结构建设：

```text
.
├── apps
│   ├── admin-web             # React 管理台
│   ├── control-api           # 控制面 API、审批、配置和查询
│   ├── feishu-gateway        # Windows 原生 WSS 接入与回复
│   └── windows-worker        # DirectTool / Agent CLI / Sandbox
├── packages
│   ├── contracts             # 事件、任务、执行器和 API 类型
│   ├── database              # PostgreSQL schema 与 migrations
│   ├── executors             # Executor Adapter 与三类执行器
│   ├── integrations          # GitLab / Confluence / 飞书适配器
│   ├── observability         # Logs / Metrics / Trace / Token
│   ├── policy                # RBAC、审批、配额和幂等
│   └── testing               # 测试夹具、模拟服务和契约测试
├── deploy
│   ├── docker                # Linux VM 控制面 Compose
│   ├── windows               # WinSW / PowerShell 安装与升级脚本
│   └── backup                # 备份、恢复和演练脚本
├── docs
├── diagrams
└── tests
    ├── e2e
    ├── load
    └── security
```

## 5. 分阶段排期

| 阶段            |        周期 | 主要交付                                 | 阶段出口                             |
| --------------- | ----------: | ---------------------------------------- | ------------------------------------ |
| P0 工程基线     |     第 1 周 | 单体仓库、契约、CI、开发环境             | 构建、测试、启动脚本全部通过         |
| P1 飞书接入 PoC |   第 2–3 周 | WSS、事件管道、回复调度                  | 私聊和群聊端到端收发稳定             |
| P2 调度与数据   |   第 4–5 周 | BullMQ、路由、会话、PostgreSQL           | 并发、幂等、重试和恢复测试通过       |
| P3 三类执行器   |   第 6–8 周 | Direct、可选 API/ReAct、Agent CLI        | 活动路由端到端通过，关闭通道强制拒绝 |
| P4 企业系统接入 |  第 9–10 周 | GitLab、Confluence、飞书只读工具         | 契约测试和权限边界通过               |
| P5 治理与审批   | 第 11–13 周 | RBAC、审批、配额、审计、脱敏             | 未授权和未审批写操作无法执行         |
| P6 管理台与运维 | 第 14–16 周 | 全部核心页面、监控、Trace、告警          | 运维人员可定位和处理典型故障         |
| P7 混合部署     | 第 17–18 周 | Windows Service、Docker 控制面、备份恢复 | 重启、升级、回滚和恢复演练通过       |
| P8 生产验收     | 第 19–20 周 | 压测、安全测试、UAT、运行手册            | 达到生产试运行准入条件               |

## 6. 阶段任务与验收标准

### P0：工程基线（第 1 周）

任务：

- `P0-01` 初始化 pnpm workspace、TypeScript、ESLint、格式化和测试框架。
- `P0-02` 建立任务、事件、执行器、审批和审计的共享契约。
- `P0-03` 建立 PostgreSQL migration、Redis 和本地 Docker 开发环境。
- `P0-04` 建立 CI：lint、typecheck、unit test、build、依赖和密钥扫描。
- `P0-05` 编写 ADR，冻结执行器协议、任务状态机和部署边界。

验收：

- 新机器可以根据文档在 30 分钟内启动开发环境。
- 所有应用具备 `/health/live` 和 `/health/ready`。
- CI 在空白功能分支上稳定通过，失败时能给出明确原因。

### P1：飞书接入 PoC（第 2–3 周）

任务：

- `P1-01` 使用飞书官方 Node SDK 建立 WSS 长连接和自动重连。
- `P1-02` 支持 `im.message.receive_v1`、私聊、群聊 `@机器人` 和卡片交互。
- `P1-03` 实现验签、事件去重、限流、幂等和附件元数据解析。
- `P1-04` 实现统一回复调度、流式状态卡片和超长消息分片。
- `P1-05` 增加断网、重连、重复事件和 OpenAPI 限流测试。

验收：

- 不开放公网端口即可持续收发飞书消息。
- 同一事件重复投递不会产生重复任务或重复回复。
- 断网恢复后自动重连，未完成任务状态可被恢复或明确标记失败。

### P2：调度与数据（第 4–5 周）

任务：

- `P2-01` 定义任务状态机：queued、running、waiting_approval、succeeded、failed、cancelled、expired。
- `P2-02` 实现 BullMQ 队列、并发、超时、退避重试、取消和死信队列。
- `P2-03` 实现规则路由器和可配置路由规则。
- `P2-04` 实现会话隔离、摘要、最近消息和任务上下文。
- `P2-05` 建立任务、事件、会话和审计数据库表。

验收：

- 多群、多用户并发任务不会串会话或串回复目标。
- Worker 异常退出后，任务按策略重试或进入死信队列。
- 路由决策、规则版本和输入摘要可被完整追踪。

### P3：三类执行器（第 6–8 周）

任务：

- `P3-01` 完成 Executor Adapter、事件流、超时、取消和错误分类。
- `P3-02` 实现 `DirectToolExecutor` 与工具网关。
- `P3-03` 实现 `ApiAgentExecutor` 的 Responses API/ReAct、工具调用、上下文限长和独立启停开关；当前关闭。
- `P3-04` 实现 `AgentCliExecutor` 的 Codex CLI JSONL 解析、会话续接和工作区绑定。
- `P3-05` 实现任务目录、ACL、资源限制、清理和 Hyper-V 沙箱接口。
- `P3-06` 建立三类执行器的路由、回退和熔断规则；关闭 API 时默认路由不得进入该通道。

验收：

- 明确命令路径不会触发任何模型调用。
- API/ReAct 适配器只能访问当前任务授权的工具集合；通道关闭时不参与路由、就绪门禁或回退。
- Agent CLI 只能访问授权工作区，并能被超时或人工取消。
- 所有执行器输出统一事件和统一审计字段。

### P4：企业系统接入（第 9–10 周）

当前状态：已关闭。共享安全层、三套只读适配器、权限边界以及 GitLab、Confluence、飞书真实全链路均已通过；31 个测试文件、86 个测试和生产依赖审计通过。GitLab 当前内网 HTTP 仅用于开发验收，生产前必须完成 TLS 改造。

任务：

- `P4-01` GitLab 只读：项目、MR、流水线、日志和代码差异。
- `P4-02` Confluence 只读：CQL、页面、附件元数据和评论。
- `P4-03` 飞书只读：文档、多维表格、群和用户信息。
- `P4-04` 统一服务账号、权限范围、重试、限流和结果截断。
- `P4-05` 为每个系统建立模拟服务和契约测试。

验收：

- 服务账号只能访问批准的项目、空间和资源。
- 工具结果在进入模型前完成限长和敏感信息脱敏。
- 外部系统超时、限流或返回异常时不会导致任务无限重试。

### P5：治理与审批（第 11–13 周）

当前状态：本地与真实联调已通过，等待提交、推送及远端门禁。RBAC、状态机、幂等、预算、审计、Windows Credential Manager 凭据引用、审批卡片发送、真实 WSS 点击回调、立即终态响应、异步更新兜底、单卡单终态、中文状态和北京时间格式均已通过；远端 CI 与 Security 成功后关闭阶段。

任务：

- `P5-01` RBAC：用户、群组、角色、工具和资源范围。
- `P5-02` 写操作分级、审批卡片、批准、拒绝、过期和撤销。
- `P5-03` 写操作幂等键和重复提交保护。
- `P5-04` 按用户、群、任务和模型的 Token 与成本预算。
- `P5-05` 审计事件、敏感字段脱敏、保留周期和导出。
- `P5-06` 凭据引用 Windows Credential Manager 或企业密钥系统。

验收：

- 未授权用户无法看到或调用未授权工具。
- 高风险写操作在批准前不会执行，批准后也只能执行一次。
- 审计记录可以还原谁、在何时、以什么权限执行了什么操作。

### P6：管理台与运维（第 14–16 周）

任务：

- `P6-01` 实现总览、任务、会话、队列、Worker 和执行器页面。
- `P6-02` 实现集成、审批、用户、角色、配额和配置页面。
- `P6-03` 实现 Token 成本、日志、Trace、告警和健康状态页面。
- `P6-04` 实现备份、发布、版本和恢复演练页面。
- `P6-05` 对取消、重试、清理、重启和回滚等操作增加风险确认。

验收：

- 运维人员能从一条失败飞书消息追踪到任务、执行器、工具和错误日志。
- 队列积压、Worker 离线、模型失败和成本超限会触发告警。
- 管理台不展示明文凭据，所有管理写操作进入审计日志。

### P7：混合部署（第 17–18 周）

任务：

- `P7-01` 将 Feishu Gateway 和 Windows Worker 托管为 Windows Service。
- `P7-02` 在 Hyper-V Linux VM 中部署 API、Web、Redis、PostgreSQL 和可观测性组件。
- `P7-03` 配置 Windows 与 Linux VM 的内部 mTLS、主机防火墙和最小端口。
- `P7-04` 实现配置升级、数据库迁移、健康检查、灰度和回滚。
- `P7-05` 实现加密备份、异机恢复和恢复演练报告。

验收：

- Windows 重启后全部服务自动恢复，并能通过健康检查。
- 生产环境没有 Docker Desktop，也没有公网入站端口。
- 在干净环境中可以根据备份恢复数据库、配置和必要运行状态。

### P8：生产验收（第 19–20 周）

任务：

- `P8-01` 并发、峰值、长任务、队列积压和限流压测。
- `P8-02` Prompt 注入、越权、路径逃逸、命令注入和凭据泄露测试。
- `P8-03` 飞书、Redis、PostgreSQL、模型和企业系统故障演练。
- `P8-04` 试点用户 UAT、缺陷修复和运行手册确认。
- `P8-05` 生产准入评审和首批用户灰度发布。

验收：

- 达到约定的并发、成功率、P95 耗时和恢复时间目标。
- 不存在未关闭的高危安全问题或数据恢复阻断问题。
- 值班人员能仅依靠运行手册完成常见故障定位、降级和恢复。

## 7. 测试策略

| 测试层     | 覆盖范围                                                |
| ---------- | ------------------------------------------------------- |
| 单元测试   | 路由、权限、配额、幂等、脱敏、状态机和错误分类          |
| 契约测试   | 飞书、GitLab、Confluence、模型 API 和 Executor 事件协议 |
| 集成测试   | Redis、PostgreSQL、队列、审批、工具网关和会话恢复       |
| 端到端测试 | 飞书消息到最终回复、审批和审计的完整链路                |
| 负载测试   | 并发用户、任务积压、长任务、限流和 Worker 扩缩容        |
| 故障测试   | 断网、依赖超时、进程崩溃、重启、数据库恢复和回滚        |
| 安全测试   | 越权、注入、路径逃逸、凭据泄露和不可信代码隔离          |

每个阶段必须先通过自动化测试和阶段出口，才能开始依赖它的下一阶段。允许并行开发，但不得绕过阶段验收。

## 8. 生产准入指标

具体数值在 P0 与业务方确认；默认基线如下：

- 月度可用性目标：99.5%。
- 飞书事件接入成功率：不低于 99.9%。
- 明确命令 P95：不高于 5 秒，不含外部系统自身耗时。
- 任务状态、工具调用和审批审计覆盖率：100%。
- 未审批高风险写操作执行数：0。
- 明文凭据进入日志、数据库和飞书消息的次数：0。
- 数据库备份恢复演练：至少每季度一次，正式上线前必须成功一次。

## 9. 外部依赖与用户配合事项

开发和测试由实施方完成，但以下事项必须由用户或公司管理员提供：

1. 飞书企业自建应用、事件权限和测试群。
2. GitLab、Confluence 和业务系统的测试服务账号。
3. Windows Server、Hyper-V、Linux VM 和管理员安装窗口。
4. 内网 DNS、VPN、代理和防火墙白名单。
5. 生产写操作范围、审批人和数据保留要求。
6. 安全评审、UAT 用户和生产上线批准。

所有密钥应由管理员直接录入 Windows Credential Manager、环境密钥文件或企业密钥系统，不应发送到聊天、Issue、Git 提交或普通日志。

## 10. 风险登记

| 风险                                    | 影响                 | 缓解措施                                               |
| --------------------------------------- | -------------------- | ------------------------------------------------------ |
| 飞书、GitLab 或 Confluence 权限审批延迟 | 阻塞真实联调         | 先使用模拟服务和契约测试；尽早提交权限申请             |
| Agent 执行不可信命令                    | 主机和数据风险       | 最小权限、工作区白名单、审批和 Hyper-V 强隔离          |
| 模型或企业系统限流                      | 延迟、失败和积压     | 队列削峰、退避重试、熔断、配额和降级                   |
| 工具结果过大或包含敏感信息              | Token 浪费和数据泄露 | 限长、摘要、脱敏和输出审计                             |
| Windows 与 Linux VM 运维复杂            | 故障定位困难         | 统一 Trace ID、健康检查、安装脚本和运行手册            |
| 备份可用但无法恢复                      | 数据恢复失败         | 自动校验、异机恢复和定期演练                           |
| 需求扩展到通用工作流平台                | 延期和范围失控       | 以三类执行器和当前页面清单为范围基线，新增需求单独评估 |

## 11. 后续开始实施时的第一批任务

后续开始开发时，严格按以下顺序执行：

1. 确认目标 Windows 版本、CPU、内存、磁盘、Hyper-V 和网络条件。
2. 确认飞书测试应用、测试群和最小权限清单。
3. 按目标工程结构初始化代码仓和共享契约。
4. 建立本地开发 Compose、数据库 migration 和 CI。
5. 先交付飞书 WSS 收发 PoC，再进入队列和执行器开发。

未完成第 1、2 项时，可以完成本地工程基线和模拟服务，但不能把飞书真实联调标记为完成。

## 12. 完成定义

项目只有同时满足以下条件才算完成：

- 三类执行器、核心集成、管理台和运维功能全部达到验收标准。
- Windows 与 Linux VM 混合部署已通过重启、升级、回滚和备份恢复测试。
- 压测、安全测试和 UAT 均完成，没有未关闭的高危问题。
- 生产运行手册、权限矩阵、数据字典、API 文档和故障处理手册齐全。
- 首批试点用户完成灰度验证，生产负责人批准进入正式运行。
